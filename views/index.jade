extends list-layout

block content  
  blockquote Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. 
  
  .nav-bar
    ul.nav
      li
        span Filter
      li
        a All
      li
        a Assembly
      li
        a Programming
      li
        a HTML5
      li
        a Clojure

  .section
    each post in posts.by_title()
      .post-summary
        h4.category-title
          img.icon(src="/img/box-icon.svg")
          if post.tags
            - var tags = post.tags && post.tags.length > 1 ? post.tags.slice(0,-1).join(", ") + " & " + post.tags.slice(-1) : post.tags[0]
            span= tags
              
        h3.post-title
          a.muted(href=post.permalink)= post.title
        .post-text!= post.body
      
